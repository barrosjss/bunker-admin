import withPWA from "@ducanh2912/next-pwa";

const pwaConfig = withPWA({
  dest: "public",
  cacheOnFrontEndNav: true,
  aggressiveFrontEndNavCaching: true,
  reloadOnOnline: true,
  swcMinify: true,
  disable: process.env.NODE_ENV === "development",
  workboxOptions: {
    disableDevLogs: true,
  },
});

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Las miniaturas y GIFs del catálogo viven en Supabase Storage. Se sirven
    // sin optimizar: ya vienen a 180x180 y los GIFs animados pierden la
    // animación si pasan por el optimizador de Next.
    remotePatterns: [
      {
        protocol: "https",
        hostname: "**.supabase.co",
        pathname: "/storage/v1/object/public/**",
      },
    ],
  },
};

// export default pwaConfig(nextConfig);
export default nextConfig;
